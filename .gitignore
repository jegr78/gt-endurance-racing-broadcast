runtime/
dist/
# the vendored Reveal.js dist is committed deck runtime, not a build output —
# keep it trackable past the unanchored dist/ rule (so a REVEAL_TAG bump that
# adds files under it does not silently skip them; see tools/fetch-reveal.py)
!src/docs/slides/vendor/reveal/dist/
incoming/
fonts.zip
# league profiles are machine-local; only the example template + the public
# demo league ship
profiles/*
!profiles/example/
!profiles/demo/
__pycache__/
*.pyc
.DS_Store
*.bak
# safety: never commit secrets/caches even if misplaced
.env
cookies.txt
*.cache.txt
*.import.json
.playwright-mcp/
.venv-pw/
.claude/scheduled_tasks.lock

# Visual-companion brainstorm sessions (local mockups)
.superpowers/

# #505 multi-feed probe output (maintainer harness; never committed)
probe-runs/
